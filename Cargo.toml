[package]
name = "slate"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.44", default-features = false, features = ["clock"] }
dirs-next = "2.0.0"
eframe = { version = "0.34.2", default-features = false, features = ["default_fonts", "glow", "x11"] }
